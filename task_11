#include <iostream>

void to_binary(int n) {
    if (n > 1) to_binary(n / 2);
    std::cout << n % 2;
}

int main() {
    int n;
    std::cin >> n;
    if (n == 0) std::cout << 0;
    else to_binary(n);
    return 0;
}
