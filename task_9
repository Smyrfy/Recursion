#include <iostream>
#include <algorithm>

int find_max(int arr[], int size) {
    if (size == 1) return arr[0];
    return std::max(arr[size - 1], find_max(arr, size - 1));
}

int main() {
    int arr[] = {3, 8, 2, 10, 4};
    std::cout << find_max(arr, 5) << std::endl;
    return 0;
}
